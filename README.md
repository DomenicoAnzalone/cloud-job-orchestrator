# cloud-job-orchestrator
Serverless platform for asynchronous CSV jobs via web UI and REST APIs: cleaning/validation, CSV-to-nested JSON (group-by), and data profiling reports. Supports multiple concurrent jobs with status/progress tracking, cancel, and output download..
